cmake_minimum_required(VERSION 3.22)

project(pigeonator)

# Include Conan toolchain to set up dependencies
include(${CMAKE_BINARY_DIR}/conan_toolchain.cmake)

find_package(OpenCV REQUIRED)
find_package(doctest REQUIRED)
find_package(CLI11 REQUIRED)
#include_directories( ${OpenCV_INCLUDE_DIRS} )

#find_package(PkgConfig REQUIRED)
#pkg_check_modules(LIBCAMERA REQUIRED libcamera)


# add shared libraries

add_subdirectory(birdhub/video)
add_subdirectory(birdhub/events)
add_subdirectory(birdhub/orchestration)
add_subdirectory(birdhub/recording)
add_subdirectory(birdhub/detection)
add_subdirectory(birdhub/memory_management)
add_subdirectory(birdhub/effectors)

# app

add_executable(pgn birdhub/birdhub.cpp)
add_executable(example birdhub/example.cpp)
add_executable(run_onnx_on_video birdhub/run_onnx_on_video.cpp)
#target_include_directories(pgn PUBLIC ${LIBCAMERA_INCLUDE_DIRS})
target_include_directories(pgn PRIVATE 
        "${PROJECT_BINARY_DIR}"
        "${PROJECT_SOURCE_DIR}/birdhub/video"
        "${PROJECT_SOURCE_DIR}/birdhub/orchestration"
        "${PROJECT_SOURCE_DIR}/birdhub/events"
        "${PROJECT_SOURCE_DIR}/birdhub/detection"
        "${PROJECT_SOURCE_DIR}/birdhub/recording"
        "${PROJECT_SOURCE_DIR}/birdhub/memory_management"
        "${PROJECT_SOURCE_DIR}/birdhub/effectors"
        ${OpenCV_INCLUDE_DIRS} )
target_include_directories(run_onnx_on_video PRIVATE 
        "${PROJECT_BINARY_DIR}"
        "${PROJECT_SOURCE_DIR}/birdhub/video"
        "${PROJECT_SOURCE_DIR}/birdhub/orchestration"
        "${PROJECT_SOURCE_DIR}/birdhub/events"
        "${PROJECT_SOURCE_DIR}/birdhub/detection"
        "${PROJECT_SOURCE_DIR}/birdhub/recording"
        "${PROJECT_SOURCE_DIR}/birdhub/memory_management"        ${OpenCV_INCLUDE_DIRS} )
target_link_libraries(pgn PRIVATE ${LIBCAMERA_LIBRARIES} opencv::opencv CLI11::CLI11 Video Events Orchestration Detection Recorder MemoryManagement Effectors)
target_link_libraries(example PRIVATE ${LIBCAMERA_LIBRARIES} opencv::opencv Video Events Orchestration Detection Recorder MemoryManagement)
target_link_libraries(run_onnx_on_video PRIVATE ${LIBCAMERA_LIBRARIES} opencv::opencv Video Events Orchestration Detection Recorder MemoryManagement)
# tests

add_subdirectory(tests/)