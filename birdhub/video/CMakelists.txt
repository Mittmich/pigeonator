add_library(Video video.hpp video.cpp)
target_include_directories(Video PUBLIC ${OpenCV_INCLUDE_DIRS})

target_include_directories(Video
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/../events
)
target_link_libraries(Video
    PUBLIC
        Events
)
target_include_directories(Video
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/../memory_management
)
target_link_libraries(Video
    PUBLIC
        MemoryManagement
)