add_library(Detection detection.hpp detection.cpp)
target_include_directories(Detection PUBLIC ${OpenCV_INCLUDE_DIRS})
target_include_directories(Detection
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/../video
)
target_link_libraries(Detection
    PUBLIC
        Video
)

target_include_directories(Detection
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/../events
)
target_link_libraries(Detection
    PUBLIC
        Events
)
target_include_directories(Detection
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/../memory_management
)
target_link_libraries(Detection
    PUBLIC
        MemoryManagement
)