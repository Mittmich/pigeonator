find_package(doctest CONFIG REQUIRED)
add_executable(test_video test_video.cpp)
target_include_directories(test_video PUBLIC
                         "${PROJECT_BINARY_DIR}"
                         "${PROJECT_SOURCE_DIR}/birdhub/video"
                         "${PROJECT_SOURCE_DIR}/birdhub/events"
                         ${OpenCV_INCLUDE_DIRS}
                         )
target_link_libraries(test_video PRIVATE doctest::doctest ${OpenCV_LIBS})
target_link_libraries(test_video PUBLIC Events Video)